test("Player have a points", () => {
  expect(sum).toEqual(2);
});

test("Player can win", () => {
  expect(sum).toEqual(2);
});

test("Player can lose", () => {
  expect(sum).toEqual(2);
});

test("Player can shoot on enemy board", () => {
  expect(sum).toEqual(2);
});

test("Player must wait for enemy turn", () => {
  expect(sum).toEqual(2);
});
