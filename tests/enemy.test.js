test("Enemy shoot on player map", () => {
  expect(sum).toEqual(2);
});

test("Enemy do not shoot in same position", () => {
  expect(sum).toEqual(2);
});

test("Enemy have a ships positioned", () => {
  expect(sum).toEqual(2);
});

test("Enemy must wait for player turn", () => {
  expect(sum).toEqual(2);
});
