test("Ship can be catched by mouse", () => {
  
  expect(sum).toEqual(2);
});

test("Ship can be rotated by mouse", () => {
  expect(sum).toEqual(2);
});

test("Ship cant be placed on other ship", () => {
  expect(sum).toEqual(2);
});

test("Ship cant be placed near other ship", () => {
  expect(sum).toEqual(2);
});

test("Ship can be placed on player map", () => {
  expect(sum).toEqual(2);
});

